// Local PostCSS config so Next.js does NOT walk up to a stray config in a
// parent/home directory. This project uses plain CSS — no plugins needed.
const config = { plugins: {} };
export default config;
